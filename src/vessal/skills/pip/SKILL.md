---
name: pip
description: Install Python packages
---

# pip

Install Python packages into the current environment.

## Methods

pip.install(package) — Install a package, returns a result string

## Usage

```python
print(pip.install("requests"))
print(pip.install("numpy>=1.20"))
```
