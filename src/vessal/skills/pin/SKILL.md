---
name: pin
description: Pin variables for observation
---

# pin

Pin variables for observation in every frame's signal section.

## Methods

pin.pin(name) — Add variable to observation
pin.unpin(name) — Remove variable from observation

The argument is the variable name as a string. If the variable does not exist, it displays "[name: not found]".
